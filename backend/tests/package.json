{
  "name": "cnstn-intranet-tests",
  "version": "1.0.0",
  "description": "QA Test Suite for CNSTN Intranet",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
